/*
// Definition for a Node.
class Node {
public:
    int val;
    Node* next;
    Node* random;
    
    Node(int _val) {
        val = _val;
        next = NULL;
        random = NULL;
    }
};
*/
#pragma once
//类的定义
class Solution {
public:
    Node* copyRandomList(Node* head);
};

